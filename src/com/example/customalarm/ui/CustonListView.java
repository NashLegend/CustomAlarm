package com.example.customalarm.ui;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.ListView;

public class CustonListView extends ListView {

	public CustonListView(Context context) {
		super(context);
		// TODO 自动生成的构造函数存根
	}

	public CustonListView(Context context, AttributeSet attrs) {
		super(context, attrs);
		// TODO 自动生成的构造函数存根
	}

	public CustonListView(Context context, AttributeSet attrs, int defStyle) {
		super(context, attrs, defStyle);
		// TODO 自动生成的构造函数存根
	}

}
